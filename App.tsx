import React, { Fragment } from 'react'
import { StatusBar } from 'expo-status-bar'

const App: React.FC = () => (
    <Fragment>
        <StatusBar style='auto' />
    </Fragment>
)

export default App
